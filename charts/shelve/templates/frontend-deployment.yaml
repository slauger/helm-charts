{{- if not .Values.frontend.image }}
{{- fail "frontend.image is required" }}
{{- end }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "shelve.fullname" . }}-frontend
  labels:
    {{- include "shelve.labels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
spec:
  replicas: {{ .Values.frontend.replicas }}
  selector:
    matchLabels:
      {{- include "shelve.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: frontend
  template:
    metadata:
      labels:
        {{- include "shelve.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: frontend
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: frontend
          image: {{ .Values.frontend.image }}
          env:
            # Das nginx-Template im Image setzt diese Variable ein; ohne sie startet
            # der Container nicht. Im Cluster leitet zwar schon der Ingress /api zum
            # Backend, lokal in Compose aber nicht - dieselbe Datei bedient beides.
            - name: BACKEND_UPSTREAM
              value: "{{ include "shelve.fullname" . }}-backend:{{ .Values.backend.port }}"
          ports:
            - name: http
              containerPort: {{ .Values.frontend.port }}
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 3
            periodSeconds: 10
          resources:
            {{- toYaml .Values.frontend.resources | nindent 12 }}
